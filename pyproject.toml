[project]
name = "jarvis-ai"
version = "0.1.0"
description = "Jarvis Personal AI Assistant"
requires-python = ">=3.10"

dependencies = [
    "openai",
    "python-dotenv"
]

[project.scripts]
jarvis = "jarvis.main:main"